from s2i import sp500, SymbolConverter

symbols = sp500.retrieve()
symbol_converter = SymbolConverter(symbols)
symbol_converter.convert(timestep, futurestep, debug=False)
vsym = s2i.yyy.convert(symbols=syms).balance()
nn = model.fit(vsym.x, vsym.y)

test = s2i.yyy.transform(symbols=AAPL) <- compute x win also for last ts
model.predict(test.x)