from s2i import stock_utils, stock_data_transformer

tickers = stock_utils.get_sp500_tickers(limit=, ratio=)
tickers_ohlcv_dict = stock_utils.get_ohlcv(ticker)
transformer = StockDataTransformer(features, timestep, futurestep)
for ticker in tickers_ohlcv_dict:
    ticker_data = tickers_data.get(ticker)
    x, y = transformer.split_time_windows(ticker, ticker_data, balance=, debug=)
